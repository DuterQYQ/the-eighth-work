//7
#include<stdio.h>
void delElem(int* arr, int n, int k)
{
	for (int i = k; i < n-1; i++)
	{
		arr[i] = arr[i + 1];
	}
	
}
int main()
{
	int arr[5] = { 1,5,62,3,99 };
	delElem(arr, 5, 3);
	for (int i = 0; i < 4; i++)
	{
		printf("%d ", arr[i]);
	}
	return 0;
}
